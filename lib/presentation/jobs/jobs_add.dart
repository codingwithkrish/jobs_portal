import 'package:flutter/material.dart';

class JobsAdd extends StatelessWidget {
  const JobsAdd({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
